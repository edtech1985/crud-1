import { styled } from "@mui/system";
import MenuItem from "@mui/material/MenuItem";

// Crie um componente MenuItem estilizado
export const StyledMenuItem = styled(MenuItem)({
  color: "primary.light",
});
